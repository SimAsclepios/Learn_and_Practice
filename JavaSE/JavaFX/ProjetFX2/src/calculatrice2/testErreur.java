package calculatrice2;



public class testErreur {

	/*
	
	Connection test=this.getConnection();
	
	if (test!=null)
	{
		Alert alert = new Alert(AlertType.INFORMATION);
		alert.setTitle("Information");
		alert.setHeaderText("Connexion à la base");
		alert.setContentText("Connexion Reussi");
      
		alert.show();
		try {
			Thread.sleep(3000);
			alert.close();
		} catch (InterruptedException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
	}
	
	else 
	{
	Alert errorAlert = new Alert(AlertType.ERROR);
	errorAlert.setHeaderText("Connexion à la base");
	errorAlert.setContentText("Echec de connexion");
	errorAlert.showAndWait();
	try {
		Thread.sleep(5000);
		errorAlert.close();
		Platform.exit();
		
	} catch (InterruptedException e) {
		// TODO Auto-generated catch block
		e.printStackTrace();
	}
	
}

}

if (test!=null) {
	//System.out.println("Connexion Réussi");
		
		Alert alert = new Alert(AlertType.INFORMATION);
		alert.setTitle("Information");
		alert.setHeaderText("Tentative de connexion");
		alert.setContentText("Connexion Réussie");
		alert.showAndWait();
		
		
	}
	
	else {
		
		Alert alert = new Alert(AlertType.INFORMATION);
		alert.setTitle("Information");
		alert.setHeaderText("Tentative de connexion");
		alert.setContentText("Connexion échouée");
		alert.showAndWait();
		
	}
	
	
	
	Connection test=this.getConnection();
		
		if (test!=null)
		{
			Alert alert = new Alert(AlertType.INFORMATION);
			alert.setTitle("Information");
			alert.setHeaderText("Connexion à la base");
			alert.setContentText("Connexion Reussi");
          
			alert.show();
			try {
				Thread.sleep(3000);
				alert.close();
			} catch (InterruptedException e) {
				// TODO Auto-generated catch block
				e.printStackTrace();
			}
		}
		
		else 
		{
		Alert errorAlert = new Alert(AlertType.ERROR);
		errorAlert.setHeaderText("Connexion à la base");
    	errorAlert.setContentText("Echec de connexion");
     errorAlert.show();
    	try {
			Thread.sleep(3000);
			errorAlert.close();
			Platform.exit();
			
		} catch (InterruptedException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
		
	}

	
	*/
	
}
